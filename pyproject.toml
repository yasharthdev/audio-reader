[project]
name = "audio-reader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyobjc>=12.2.1",
    "pyttsx3>=2.99",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
