# Audio-Reader

Welcome to the repository! 

Currently, the core `main` branch is a clean slate reserved for future manual development. 

However, a fully functional, AI-assisted prototype of the application (featuring edge-tts, JSON state management, and Obsidian export) is available on a separate branch. 

## How to view and run the prototype:
To access the working code, switch to the experimental branch after cloning:

```bash
git switch ai-experimental
